"""
dataloader — EEG data pipeline package.

Contains:
    core/    — signal processing (augment, bids, cache, channels, etc.)
    loader/  — dataset loaders, windowing, splitting, tensor generation
"""