# Legacy Baseline Scripts

This directory contains the **original baseline training scripts and results** that predate the v2 improvements (Focal loss, channel attention, etc.).

They are **no longer used by the current training pipeline** but are kept temporarily for reference. You can safely delete this folder after validating that the new pipeline meets your needs:

```bash
# to remove later
rm -rf src/models/legacy_baseline
```
